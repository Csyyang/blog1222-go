package comment

import (
	"github.com/gin-gonic/gin"
)

func Links(c *gin.Context) {
	// db := mysql.Db

	// sqStr := "update comment set praise_num = praise_num + 1 where comment_id = ?"

	// _, err3 := db.Exec(viweSql, oldId+1, articleId["id"])
	// if err3 != nil {
	// 	fmt.Println("更新失败")
	// 	c.JSON(500, "bad")
	// 	return
	// }
}
